#include "ConcurrentAlloc.h"

// 对外分配接口故意放在头文件里做内联。
// 这样做的目的，是让编译器更容易把热路径直接展开到调用点，
// 尤其是小对象分配/释放这种极高频路径，可以减少一次函数调用开销。
//
// 这个 .cpp 文件仍然保留：
// 1. 让工程结构保持完整；
// 2. 避免在 VS 工程里看起来像“少了实现文件”；
// 3. 也方便后续如果想把部分逻辑重新移回 .cpp，有稳定的落点。
